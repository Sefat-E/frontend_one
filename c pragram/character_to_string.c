#include <stdio.h>
int main()
{
    char str[20];
    int n;
    scanf("%d", &n);
    scanf("%s", str);


        int digit = str[i] - '0';

        num = num * 10 + digit;
    }
    printf("%d\n", num);

    return 0;
}