#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
   
    char str1[100], str2[100], str3[100];
    
    fgets(str1, sizeof(str1), stdin);
    str1[strcspn(str1, "\n")] = '\0'; 
    
    fgets(str2, sizeof(str2), stdin);
    str2[strcspn(str2, "\n")] = '\0';
   
    fgets(str3, sizeof(str3), stdin);
    str3[strcspn(str3, "\n")] = '\0';
    
    int len = strlen(str1);
    // int count1 = 0;  int count2 = 0; int sum = 0;
    int count = 0;
     for (int i = 0; i < len; i++) {
         if (str1[i] == str2[i] && str2[i] == str3[i] && str1[i] == str3[i]) {
            
            continue;
        }
        
        if (str1[i] == str2[i] || str2[i] == str3[i] || str1[i] == str3[i]){
             count +=1;
        }
        
        // if(str1[i] != str3[i]){
        //     // str3[i] = str1[i];
        //       count +=1;
        // }
       
        if (str2[i] != str3[i] && str1[i] != str2[i] && str1[i] != str3[i]) {
        // if (str2[i] != str3[i]){
          count +=2;
         }
        
     }  
     printf("%d",  count);
    return 0;
}
    
    

